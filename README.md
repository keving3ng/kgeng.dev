# kgeng.dev

Personal site: React, TypeScript, Vite, Tailwind, Cloudflare Pages (Functions + Notion-backed content).

**Handbook for contributors and coding agents:** [AGENTS.md](./AGENTS.md) (commands, architecture, conventions, verification).

```bash
npm ci
npm run check    # tsc + vite build + eslint + vitest
npm run dev      # full stack, http://localhost:8788
```

Node version: see [`.nvmrc`](./.nvmrc).
